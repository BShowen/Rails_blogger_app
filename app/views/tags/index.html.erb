<h1>All Tags</h1>

<ul id="articles"> 
  <% @tags.each do |tag| %>
    <li>
        <%= link_to tag.name , tag_path(tag.id) %>
        <%= link_to "Delete" , tag_path(tag.id), method: :delete %>
    </li>
  <% end %>
</ul>

<%= link_to '<< Back to All Articles' , articles_path %>